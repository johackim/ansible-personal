---
- name: Installation
  hosts: '{{ target }}'
  vars_files:
    - vars/main.yml

  roles:
    - apt
    - user
    - motd
    - firewall
    - fail2ban
    - { role: docker, when docker.stat.exists == false }
...
