---
- name: Installation
  hosts: all
  vars_files:
    - vars/main.yml

  roles:
    - apt
    - motd
    - firewall
    - fail2ban
    - nodejs
    - unattended-upgrades
    - docker
    - nginx
    - dokku # NOTICE: Keep this role after nginx
    - ssh-hardening
    - os-hardening
    - user # NOTICE: Keep this role after ssh-hardening
...
